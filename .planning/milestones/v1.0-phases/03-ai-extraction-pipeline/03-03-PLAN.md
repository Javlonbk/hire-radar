---
phase: 03-ai-extraction-pipeline
plan: "03"
type: execute
wave: 3
depends_on: ["03-02"]
files_modified:
  - src/db/schema.sql
  - src/db/raw-items.ts
  - src/db/raw-items.test.ts
  - src/db/vacancies.ts
  - src/db/vacancies.test.ts
  - src/extraction/pipeline.ts
  - src/extraction/pipeline.test.ts
autonomous: true
requirements: [EXTR-01, EXTR-02, EXTR-04]

must_haves:
  truths:
    - "Each run pulls raw_items WHERE extraction_status='pending', processes them, and marks done/failed — resumable & idempotent"
    - "A multi-job post produces multiple vacancy rows; a non-job post produces zero rows and the raw item is marked done"
    - "Running the pipeline twice on identical raw content inserts exactly ONE vacancy row (dedup hash + INSERT OR IGNORE)"
    - "A hard API error leaves the raw item pending (retried next run); a validation-skip marks it failed"
    - "Each persisted vacancy row records its lang (uz-Latn|uz-Cyrl|ru|en) — the extracted lang is written, not dropped"
  artifacts:
    - path: "src/db/schema.sql"
      provides: "vacancies.lang column added to the CREATE TABLE statement"
      contains: "lang"
    - path: "src/db/raw-items.ts"
      provides: "getPendingRawItems + markExtractionStatus (added to existing file)"
      exports: ["getPendingRawItems", "markExtractionStatus"]
    - path: "src/db/vacancies.ts"
      provides: "insertVacancies — maps Vacancy[] to rows with dedup content_hash + INSERT OR IGNORE, binds lang"
      exports: ["insertVacancies"]
    - path: "src/extraction/pipeline.ts"
      provides: "extractPending(deps) — the phase-level function Phase 4 CLI calls"
      exports: ["extractPending"]
  key_links:
    - from: "src/extraction/pipeline.ts"
      to: "src/extraction/extractor.ts"
      via: "extractOne per pending raw item"
      pattern: "extractOne"
    - from: "src/db/vacancies.ts"
      to: "vacancies.content_hash UNIQUE"
      via: "contentHash(normalize(title+company+desc-prefix)) + INSERT OR IGNORE"
      pattern: "INSERT OR IGNORE INTO vacancies"
    - from: "src/db/vacancies.ts"
      to: "vacancies.lang"
      via: "lang in INSERT column list, bound from v.lang"
      pattern: "lang"
    - from: "src/extraction/pipeline.ts"
      to: "raw_items.extraction_status"
      via: "markExtractionStatus done|failed; pending left on hard error"
      pattern: "markExtractionStatus"
---

<objective>
Wire the pipeline: pull pending raw_items, run extractOne on each, map vacancies to rows with a normalized dedup hash and INSERT OR IGNORE, persist the extracted lang, and transition extraction_status. Exposes extractPending — the phase-level function the Phase 4 CLI will call. NO CLI command, flags, or stats printing here (Phase 4).

Purpose: Closes EXTR-02 (multi-job → N rows, non-job → 0 rows) and EXTR-04 (dedup → exactly one row on re-run), completes the resumable pending→done/failed loop, and persists `lang` (locked decision "record lang"; ROADMAP Success Criterion 1 "language correctly populated"). Reuses Phase 1 normalize/contentHash for the dedup key (PITFALL 7 — normalize before hash).
Output: vacancies.lang column, getPendingRawItems + markExtractionStatus, insertVacancies (binding lang), extractPending, with tests proving the success criteria end-to-end against a fake client.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-ai-extraction-pipeline/03-CONTEXT.md
@.planning/phases/03-ai-extraction-pipeline/03-02-SUMMARY.md

<interfaces>
<!-- Source-of-truth. Use directly. -->

From src/db/schema.sql (raw_items + vacancies columns — you ADD a lang column to vacancies; see Task 1):
```sql
raw_items( id, source_id, external_id, raw_text, raw_json, content_hash,
           fetched_at, extraction_status DEFAULT 'pending', extraction_error )
vacancies( id, source_id, raw_item_id, external_id, title, company, description,
           skills, salary_min, salary_max, salary_currency, location, remote_type,
           apply_contact, posted_at, content_hash UNIQUE, created_at )
```
Note: vacancies.skills is TEXT → store JSON.stringify(skills[]). title/company/description are NOT NULL.

From src/db/client.ts:
```typescript
export function runInTransaction<T>(db, fn: () => T): T; // SYNCHRONOUS — no await inside
```

From src/db/raw-items.ts (existing — you APPEND functions, keep persistRawItems):
```typescript
export function persistRawItems(db, items: RawItem[]): number; // INSERT OR IGNORE pattern to mirror
```

From src/extraction/extractor.ts (Wave 2):
```typescript
export interface ExtractDeps { db; messageCreate; model; system; now?; log?; }
export interface ExtractOutcome { result: ExtractionResult | null; fromCache; apiCalls; skipped; }
export async function extractOne(deps, rawText: string): Promise<ExtractOutcome>;
export const EXTRACTION_SYSTEM_PROMPT: string;
```

From src/extraction/schema.ts:
```typescript
export type Vacancy = { title; company; description; location; remote_type; salary_min;
  salary_max; salary_currency; skills: string[]; apply_contact; lang };
```

From src/hash.ts:
```typescript
export function normalize(text: string): string;
export function contentHash(text: string): string; // sha256 of normalize(text)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: vacancies.lang column + raw_items read/status helpers + vacancy insert with dedup</name>
  <files>src/db/schema.sql, src/db/raw-items.ts, src/db/raw-items.test.ts, src/db/vacancies.ts, src/db/vacancies.test.ts</files>
  <read_first>
    - src/db/schema.sql (vacancies CREATE TABLE — you add the lang column here; also raw_items columns, NOT NULL constraints, content_hash UNIQUE)
    - src/db/raw-items.ts (existing persistRawItems — mirror prepared-statement + runInTransaction style; append, do not rewrite)
    - src/db/client.ts (runInTransaction synchronous)
    - src/hash.ts (normalize + contentHash for the dedup key)
    - src/extraction/schema.ts (Vacancy field names — snake_case from the model; lang is required: 'uz-Latn'|'uz-Cyrl'|'ru'|'en')
  </read_first>
  <behavior>
    - getPendingRawItems(db, limit?) returns rows WHERE extraction_status='pending' with id, source_id, raw_text (and external_id) — ordered by fetched_at; respects optional limit.
    - markExtractionStatus(db, id, status, error?) sets extraction_status and extraction_error for the given raw item id; status is 'done' | 'failed'.
    - insertVacancies(db, { rawItemId, sourceId, externalId }, vacancies: Vacancy[]) returns the number of rows actually inserted. For each vacancy: dedupKey = contentHash(normalize(title) + '|' + normalize(company) + '|' + normalize(description.slice(0,200))); INSERT OR IGNORE INTO vacancies(...) with content_hash=dedupKey, skills=JSON.stringify(skills), and lang=v.lang. All inserts in one runInTransaction.
    - Test: insertVacancies with the SAME vacancy twice (two calls, or one array with a duplicate) → exactly ONE row (assert COUNT==1, second call returns 0). (EXTR-04)
    - Test: a trailing-space / Cyrillic-homoglyph variant of title+company+desc-prefix hashes identically → still one row (normalize-before-hash, PITFALL 7).
    - Test: inserting a 3-vacancy array creates 3 rows linked to the same raw_item_id.
    - Test: an inserted vacancy row has lang === the value supplied on the Vacancy (e.g. 'uz-Latn') — the extracted lang is persisted, not dropped. (Success Criterion 1)
    - Test: getPendingRawItems returns only pending rows; after markExtractionStatus(id,'done') that row is no longer pending.
  </behavior>
  <action>
    Modify src/db/schema.sql:
    - Add a `lang TEXT` column to the `vacancies` CREATE TABLE statement, placed logically after `apply_contact` (i.e. `apply_contact TEXT,` then `lang TEXT,` then `posted_at TEXT,`). The schema uses `CREATE TABLE IF NOT EXISTS` and there is no production DB and no migration system, so adding the column directly to the CREATE TABLE statement is correct — fresh DBs (including the `:memory:` test DB opened by openDatabase) pick it up automatically. Existing dev DB files must be recreated to gain the column; this is acceptable (pre-release, no real data). Do NOT add a migration mechanism.

    Append to src/db/raw-items.ts (keep existing persistRawItems and its imports):
    - `export interface PendingRawItem { id: string; sourceId: string; externalId: string | null; rawText: string }`.
    - `export function getPendingRawItems(db, limit?: number): PendingRawItem[]` — `SELECT id, source_id, external_id, raw_text FROM raw_items WHERE extraction_status = 'pending' ORDER BY fetched_at ASC` + optional `LIMIT ?`. Map columns to camelCase.
    - `export function markExtractionStatus(db, id: string, status: 'done' | 'failed', error?: string): void` — `UPDATE raw_items SET extraction_status = ?, extraction_error = ? WHERE id = ?` (error ?? null).

    Create src/db/vacancies.ts:
    - `import { normalize, contentHash } from '../hash.js'` and the Vacancy type from '../extraction/schema.js'.
    - `export function insertVacancies(db, link: { rawItemId: string; sourceId: string; externalId: string | null }, vacancies: Vacancy[]): number`.
    - Prepared stmt: `INSERT OR IGNORE INTO vacancies (source_id, raw_item_id, external_id, title, company, description, skills, salary_min, salary_max, salary_currency, location, remote_type, apply_contact, lang, content_hash) VALUES (?,?,?,?,?,?,?,?,?,?,?,?,?,?,?)`.
    - dedupKey: `contentHash(normalize(v.title) + '|' + normalize(v.company) + '|' + normalize(v.description.slice(0, 200)))`. (contentHash already normalizes internally; the explicit normalize on each field + slice makes the prefix boundary deterministic regardless of homoglyphs/whitespace.)
    - skills column: `JSON.stringify(v.skills)`. lang column: bind `v.lang` directly (the Vacancy schema guarantees one of the four enum values). Map nulls straight through (location, remote_type, salary_*, apply_contact). Store the FULL description; only the HASH uses the 200-char prefix.
    - Wrap the loop in runInTransaction; sum `result.changes` and return the total.

    Write src/db/raw-items.test.ts additions (or a new section) and src/db/vacancies.test.ts using openDatabase(':memory:'). Seed a raw_items row first (persistRawItems or a direct insert) so raw_item_id is valid. Cover every behavior bullet, including the lang-persisted assertion.
  </action>
  <verify>
    <automated>npx vitest run src/db/raw-items.test.ts src/db/vacancies.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/db/raw-items.test.ts src/db/vacancies.test.ts` passes.
    - `grep -E "lang[[:space:]]+TEXT" src/db/schema.sql` matches (lang is a column on the vacancies table).
    - `grep -E "\blang\b" src/db/vacancies.ts` matches (lang is in the INSERT column list and bound from the vacancy).
    - The vacancies INSERT column list contains `lang` and the bound values include `v.lang` (the lang-persisted test reads it back === the supplied value, e.g. 'uz-Latn').
    - `grep -c "INSERT OR IGNORE INTO vacancies" src/db/vacancies.ts` is 1.
    - `grep -E "normalize\(" src/db/vacancies.ts` matches (normalize used in dedup key).
    - Dedup test asserts: inserting the same vacancy twice → COUNT(*) of vacancies == 1 and the second insertVacancies call returns 0.
    - Homoglyph/whitespace-variant test asserts still one row.
    - Multi-vacancy test asserts 3 rows with the same raw_item_id.
    - `grep -E "WHERE extraction_status = 'pending'" src/db/raw-items.ts` matches.
  </acceptance_criteria>
  <done>vacancies.lang column added to schema.sql; getPendingRawItems + markExtractionStatus added; insertVacancies maps Vacancy[]→rows with normalized dedup hash + INSERT OR IGNORE (duplicate → one row), binding lang; multi-job → N rows; inserted row's lang round-trips; tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: extractPending — the phase-level pipeline function</name>
  <files>src/extraction/pipeline.ts, src/extraction/pipeline.test.ts</files>
  <read_first>
    - src/extraction/extractor.ts (extractOne, ExtractDeps, ExtractOutcome, EXTRACTION_SYSTEM_PROMPT)
    - src/db/raw-items.ts (getPendingRawItems, markExtractionStatus — Task 1)
    - src/db/vacancies.ts (insertVacancies — Task 1)
    - src/pipeline/orchestrator.ts (mirror the injected db/now/log + per-item try/catch + stats-array style)
    - .planning/research/PITFALLS.md Pitfall 10 (no await inside transaction — do extraction async first, write sync) and the "Looks Done But Isn't" checklist (cache before call; non-job discarded; dedup)
    - .planning/phases/03-ai-extraction-pipeline/03-CONTEXT.md (status-transition rules: non-job→done 0 vacancies; validation skip→failed; hard API error→leave pending)
  </read_first>
  <behavior>
    - extractPending(deps) pulls getPendingRawItems, and for EACH item runs extractOne(extractDeps, rawText). It accumulates stats { processed, vacanciesInserted, nonJob, skipped, apiCalls, errors }.
    - Per-item disposition:
      - extractOne returns result with is_job_post=true → insertVacancies(link, result.vacancies); markExtractionStatus(id,'done'); add inserted count.
      - is_job_post=false (non-job) → insert nothing; markExtractionStatus(id,'done'); nonJob++.
      - outcome.skipped (validation failed twice) → markExtractionStatus(id,'failed', 'extraction validation failed'); skipped++.
      - extractOne THROWS (hard API/network error) → catch, leave status PENDING (do not mark), errors++, log, CONTINUE to next item.
      - accumulate outcome.apiCalls.
    - Idempotency: a second extractPending run over the same DB makes ZERO new API calls for already-cached content AND inserts ZERO new vacancies (dedup). Both asserted.
    - The async/sync boundary (PITFALL 10): await extractOne FIRST (async), then the DB writes (insertVacancies/markExtractionStatus are sync, each already wrapped in runInTransaction). No await inside any transaction.
    - Test (end-to-end, fake counting messageCreate, :memory: db):
      - Seed 1 multi-job pending raw → extractPending → N vacancy rows, raw marked done, item no longer pending. (EXTR-02 multi-job)
      - Seed 1 non-job pending raw → 0 vacancies, raw marked done, nonJob==1. (EXTR-02 non-job)
      - Run extractPending TWICE on the same seed → second run apiCalls==0 (cache) AND total vacancy rows unchanged (dedup). (Success Criteria 3 + 4)
      - A raw whose fake throws → that raw stays pending, errors==1, other raws still processed. (hard-error resilience)
      - A raw whose fake returns invalid-twice → marked failed, skipped==1.
  </behavior>
  <action>
    Create src/extraction/pipeline.ts.

    ```
    export interface PipelineDeps {
      db: Database.Database;
      messageCreate: MessageCreate;   // injected — fake in tests, realMessageCreate in Phase 4
      model: string;
      limit?: number;
      now?: () => Date;
      log?: (msg: string) => void;
    }
    export interface PipelineStats {
      processed: number; vacanciesInserted: number; nonJob: number;
      skipped: number; apiCalls: number; errors: number;
    }
    export async function extractPending(deps: PipelineDeps): Promise<PipelineStats>;
    ```
    Implementation:
    1. `const log = deps.log ?? console.log; const stats = { processed:0, vacanciesInserted:0, nonJob:0, skipped:0, apiCalls:0, errors:0 };`
    2. `const pending = getPendingRawItems(deps.db, deps.limit);`
    3. Build `extractDeps: ExtractDeps = { db: deps.db, messageCreate: deps.messageCreate, model: deps.model, system: EXTRACTION_SYSTEM_PROMPT, now: deps.now, log }`.
    4. For each item: `try { const outcome = await extractOne(extractDeps, item.rawText); stats.apiCalls += outcome.apiCalls; ... }` then branch per the disposition rules above; `catch (err) { stats.errors++; log(\`pipeline: \${item.id.slice(0,8)} hard error, left pending: \${msg}\`); continue; }` — do NOT mark status in the catch (leave pending).
    5. `stats.processed++` for every item that did not hard-error (or count all attempts — pick processed = items that reached a terminal status; document the choice).
    6. Pass the link `{ rawItemId: item.id, sourceId: item.sourceId, externalId: item.externalId }` to insertVacancies.
    7. Return stats.
    Do NOT print per-run stats banners, add CLI flags, or read config here — extractPending receives model + messageCreate from its caller (Phase 4 wires realMessageCreate + loadConfig). This is the phase's public function, not a command.

    Write src/extraction/pipeline.test.ts: a fake messageCreate (vi.fn) whose return varies by input rawText (route by a substring) so one test DB can host multi-job / non-job / throwing / invalid raws simultaneously. Seed raw_items via a direct INSERT or persistRawItems. Assert every behavior bullet, especially the twice-run (apiCalls==0 second run, vacancy COUNT stable).
  </action>
  <verify>
    <automated>npx vitest run src/extraction/pipeline.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/extraction/pipeline.test.ts` passes.
    - Twice-run test asserts the fake messageCreate call count does NOT increase on the second extractPending (0 new API calls — Success Criterion 3) AND `SELECT COUNT(*) FROM vacancies` is identical before/after the second run (Success Criterion 4).
    - Multi-job test asserts N>1 vacancy rows for one raw item and that raw's status=='done'.
    - Non-job test asserts 0 vacancies inserted and status=='done' and nonJob==1.
    - Hard-error test asserts the throwing raw's status is still 'pending' and errors==1 and remaining raws processed.
    - Invalid-twice test asserts status=='failed' and skipped==1.
    - `grep -E "no .*await|runInTransaction" src/extraction/pipeline.ts` — confirm extractPending does NOT call runInTransaction with an async fn (await only around extractOne, outside any transaction).
  </acceptance_criteria>
  <done>extractPending loops pending raws, extracts (cache-gated) → inserts deduped vacancies → transitions status (done/failed, pending on hard error); twice-run = 0 new API calls + 0 new rows; multi-job and non-job handled; tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| model output → SQLite vacancies | LLM-produced fields written to DB; is_job_post gate + Zod (Wave 2) already filter |
| raw post text → logs | post bodies must not be logged at info level (PII) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-07 | Spoofing/Tampering (fabricated rows) | pipeline.ts insert path | mitigate | Only is_job_post=true results reach insertVacancies; non-job → zero rows + status done. Asserted by the non-job test (0 rows) |
| T-03-08 | Information disclosure | pipeline.ts logging | mitigate | Logs use item.id prefix + counts only; never the raw_text body or full model result. Verify no `log(item.rawText)` |
| T-03-09 | Denial of service / data loss (re-extraction & dup pollution) | extractPending idempotency | mitigate | Cache gate (Wave 2) + content_hash UNIQUE dedup ensure twice-run = 0 new calls + 0 new rows; hard errors leave items pending for safe retry rather than dropping them. Verified by twice-run + hard-error tests |
</threat_model>

<verification>
- `npx vitest run src/extraction/` and `src/db/vacancies.test.ts src/db/raw-items.test.ts` all pass.
- `npx vitest run` (full suite) stays green — no regression to Phase 1/2 tests.
- `npx tsc --noEmit` exits 0.
- schema.sql changed ONLY by adding the `lang TEXT` column to the vacancies table (no other tables touched, no migration mechanism).
- No live API: every extraction test uses an injected fake messageCreate.
</verification>

<success_criteria>
- Pending→done/failed loop is resumable & idempotent (locked decision).
- Multi-job → N rows; non-job → 0 rows, raw marked done (EXTR-02, Success Criterion 2).
- Twice-run → exactly one Claude call total and exactly one vacancy row (Success Criteria 3 + 4).
- Hard API error leaves the raw pending; validation skip marks it failed (locked decision).
- Extracted lang is persisted on each vacancy row (locked "record lang"; Success Criterion 1 "language correctly populated").
</success_criteria>

<output>
After completion, create `.planning/phases/03-ai-extraction-pipeline/03-03-SUMMARY.md`
</output>
</output>
